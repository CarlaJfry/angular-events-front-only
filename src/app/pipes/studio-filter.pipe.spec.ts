import { StudioFilterPipe } from './studio-filter.pipe';

describe('StudioFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudioFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
